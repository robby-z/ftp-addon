#include <tunables/global>

profile reolink_ftps flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  capability chown,
  capability dac_override,
  capability fowner,
  capability net_bind_service,
  capability setgid,
  capability setuid,
  capability sys_chroot,
  file,
  network inet stream,
  network inet6 stream,
  signal (send,receive),

  /init ix,
  /bin/** ix,
  /sbin/** ix,
  /usr/bin/** ix,
  /usr/sbin/** ix,
  /usr/local/bin/reolink-ftps rix,
  /usr/lib/bashio/** rix,
  /package/** rix,
  /command/** rix,
  /etc/services.d/** rix,
  /run/{,**} rwk,
  /tmp/{,**} rwk,
  /dev/{null,random,urandom,tty} rw,
  /proc/sys/{fs/file-max,kernel/ngroups_max} r,

  /data/{,**} rwk,
  /media/{,**} rwk,
  /share/{,**} rwk,
  /ssl/{,**} r,
  /etc/{passwd,group,shadow,gshadow,shells} rw,
  /etc/{passwd-,group-,shadow-,gshadow-} rw,
  /etc/vsftpd/{,**} rwk,
  /etc/pam.d/vsftpd r,
  /etc/ssl/{,**} r,
}
